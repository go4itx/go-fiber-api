package controller

import (
	"home/internal/{{.Name}}/service"
	"home/pkg/resp"

	"github.com/gofiber/fiber/v2"
)

type {{.Name}} struct {
}

// index ...
func (*{{.Name}}) index(ctx *fiber.Ctx) (err error) {
	return resp.New(ctx).JSON(service.{{.Name2came}}.Index())
}
